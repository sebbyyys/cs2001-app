import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#222222",
    },
    secondary: {
      main: "#fff",
    },
  },
  background: {
    default: "#fff",
  },
});
export default theme;
